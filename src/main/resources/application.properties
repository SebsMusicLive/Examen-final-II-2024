spring.application.name=examen_final
server.port=9090

# Configuración de conexión a la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/examen_final
spring.datasource.username=root
spring.datasource.password=

# Dialecto de Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Mostrar consultas SQL en la consola (opcional para depuración)
spring.jpa.show-sql=true

# Generar automáticamente el esquema de la base de datos (puedes usar update/create/drop)
spring.jpa.hibernate.ddl-auto=update

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
